/**
 * TheRollNumber Wallet - React Native Entry Point
 * Compatible with React Native 0.76 + Reanimated v3
 */

import React from 'react';
import { AppRegistry } from 'react-native';
import App from './App.jsx';

// Register the main component for React Native
AppRegistry.registerComponent('AwesomeProject4', () => App);

export default App;